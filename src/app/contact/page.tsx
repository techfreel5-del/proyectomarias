import { ContactContent } from '@/components/customer/ContactContent';

export const metadata = { title: 'Contact · MARIASCLUB™' };

export default function ContactPage() {
  return <ContactContent />;
}
